{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop window capability for Lattice foundation commands.",
  "windows": ["main"],
  "permissions": ["core:default", "log:default"]
}
