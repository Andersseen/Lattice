{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capability for Lattice foundation metadata, settings, and runtime discovery.",
  "windows": ["main"],
  "permissions": [
    "allow-get-app-info",
    "allow-application-settings",
    "allow-model-runtime-discovery"
  ]
}
