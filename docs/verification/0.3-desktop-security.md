# 0.3 Desktop Security Verification

Date: 2026-09-07

## Automated Evidence

- `pnpm dlx @fission-ai/openspec@1.12.0 validate --all --strict --no-interactive`: passed.
- `cargo test -p lattice-desktop`: passed 5 tests, including CSP/capability config and startup failure exit-policy coverage.
- `pnpm check`: passed format, lint, typecheck, unit tests, `cargo check --workspace`, and `cargo clippy --workspace --all-targets -- -D warnings`.
- `pnpm build:web`: passed. Production bundle total: 386.86 kB raw / 99.25 kB estimated transfer.
- `pnpm --filter @lattice/desktop tauri:build`: passed. Built no-bundle release binary at `target/release/lattice-desktop`.
- `pnpm e2e`: passed 2 browser-shell Playwright tests.
- `pnpm audit --audit-level high`: passed with no known vulnerabilities.
- `cargo audit --deny yanked`: passed, with 17 allowed RustSec warnings requiring triage before stricter release qualification.

Allowed RustSec warnings observed:

- GTK3 binding unmaintained warnings: `RUSTSEC-2024-0411` through `RUSTSEC-2024-0420` for `atk`, `atk-sys`, `gdk`, `gdk-sys`, `gdkwayland-sys`, `gdkx11`, `gdkx11-sys`, `gtk`, `gtk-sys`, and `gtk3-macros`.
- `RUSTSEC-2024-0370`: `proc-macro-error` unmaintained.
- `RUSTSEC-2025-0075`, `RUSTSEC-2025-0080`, `RUSTSEC-2025-0081`, `RUSTSEC-2025-0098`, `RUSTSEC-2025-0100`: `unic-*` crates unmaintained.
- `RUSTSEC-2024-0429`: `glib` unsoundness warning.

Tauri build artifact inspection confirmed generated app ACL metadata for `allow-get-app-info` and `deny-get-app-info`, and release capabilities containing only `allow-get-app-info` for `main`.

## Native Evidence

Pending manual or automated native WebView evidence:

- Production Home/System rendering under CSP.
- Metadata IPC success from the trusted main window.
- Blocked remote script load.
- Blocked unapproved remote connection while `ipc:` remains available.
- Unauthorized window/origin command invocation denial.

## P0 Resource Baseline

Pending P0 measurement:

- Release build.
- No runtime/model/MCP/provider/workspace/task capability active.
- Five-minute settled idle sample.
- Three launch/exit cycles.
- Native/WebView aggregate physical memory and CPU.
- Child process list.
- Hardware, OS, build profile, command versions, and uncertainty notes.
