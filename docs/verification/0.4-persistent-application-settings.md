# 0.4 Persistent Application Settings Verification

Recorded on 2026-09-07 from the active implementation branch.

## Scope Verified

- Rust-owned settings contracts, validation, revision conflicts, SQLite migrations, rollback, newer-schema refusal, backup creation, and storage preparation errors.
- Generated TypeScript bindings and checked command inventory for `get_app_settings`, `update_app_settings`, and `reset_app_settings`.
- Tauri managed settings state, OS app-data path resolution, and explicit main-window settings permission.
- Angular settings decoder, browser smoke fallback, Settings route, save/reset/retry states, and appearance application.

## Automated Checks

- `cargo test -p lattice-core`: 11 passed.
- `cargo test --workspace`: 16 passed.
- `cargo check --workspace`: passed.
- `cargo clippy --workspace --all-targets -- -D warnings`: passed.
- `pnpm --filter @lattice/desktop test`: 5 files / 13 tests passed.
- `rtk proxy pnpm --filter @lattice/desktop typecheck`: passed. A previous non-proxy `rtk pnpm --filter @lattice/desktop typecheck` reported exit code 1 while printing `TypeScript: No errors found`; proxy mode showed the underlying command passing.
- `pnpm lint`: passed.
- `pnpm build:web`: passed.
- `pnpm check`: passed.
- `pnpm e2e`: 3 Chromium browser smoke tests passed, including Settings edit/save in browser fallback mode.
- `pnpm openspec:validate`: permanent specs passed.
- `pnpm dlx @fission-ai/openspec@1.12.0 validate persistent-application-settings --strict`: active change passed.
- `pnpm --filter @lattice/desktop tauri:build`: passed and produced `target/release/lattice-desktop` with `--no-bundle`.

## Evidence Limits

The automated restart persistence evidence is covered at the Rust repository level by reopening the same temporary SQLite database. A live GUI restart smoke against the built Tauri app was not run in this session. Browser E2E uses the in-memory fallback and is not native persistence evidence.

The environment emitted repeated npm warnings about `${NPM_TOKEN}` not being set in the user npm config. They did not block verification.
