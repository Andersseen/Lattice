# 0.5 ModelRuntime Discovery Verification

Recorded on 2026-09-07 from branch `v1-roadmap-work`.

## Scope Verified

- Rust-owned ModelRuntime discovery DTOs and checked IPC command names.
- Absolute runtime executable validation, explicit probe command, fixed read-only argv arrays, 2 second timeout, bounded output capture, and no shell execution.
- Fixture parsing for `lms --version`, `lms daemon status --json`, and `lms server status --json --quiet`.
- Distinct missing, unsupported, stopped, running, unreachable, and unknown status handling with separate daemon/server observations.
- SQLite schema version 2 migration, persisted runtime metadata, backup on migration, newer-schema refusal, and changed-executable approval invalidation.
- Tauri managed state and explicit main-window permission for runtime discovery commands.
- Angular decoder, browser fallback, ModelRuntime store, Models route, configure/probe actions, and browser smoke coverage.

## Automated Checks

- `cargo test -p lattice-core`: 25 passed. One reachable-loopback fixture test skips only when the local sandbox denies binding a loopback listener.
- `cargo test --workspace`: 30 passed.
- `cargo clippy --workspace --all-targets -- -D warnings`: passed.
- `pnpm --filter @lattice/desktop test`: 6 files / 19 tests passed.
- `rtk proxy pnpm --filter @lattice/desktop typecheck`: passed.
- `pnpm build:web`: passed.
- `pnpm check`: passed.
- `pnpm e2e`: 4 Chromium browser smoke tests passed, including runtime configure/probe in browser fallback mode.
- `pnpm --filter @lattice/desktop tauri:build`: passed and produced `target/release/lattice-desktop` with `--no-bundle`.

## Documentation Checks

- `pnpm dlx @fission-ai/openspec@1.12.0 validate model-runtime-discovery --strict`: passed.
- `pnpm openspec:validate`: permanent specs passed.

## Evidence Limits

No real `lms`/llmster executable was invoked in this session. Real runtime discovery remains opt-in evidence before final archive. Browser E2E uses the in-memory fallback and is not native process evidence. The native build verifies compile/configuration, not a live GUI probe.

The environment emitted repeated npm warnings about `${NPM_TOKEN}` not being set in the user npm config. They did not block verification.
