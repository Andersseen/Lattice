# Tasks

- [ ] Add `AppError::conversation_not_found` (`conversation.not_found`) and `AppError::conversation_conflict` (`conversation.conflict`) constructors in `crates/lattice-core/src/error.rs`, following the exact fixed-`&'static str`-message pattern the existing constructors use.
- [ ] Add `crates/lattice-core/src/conversations/mod.rs`: `GenerationStatus`, `Message`, `Conversation`, `ConversationSummary`, `new_conversation_id`/`new_message_id`, title-derivation helper, `CHECKPOINT_DELTA_BATCH`/`CHECKPOINT_MIN_INTERVAL` constants, IPC request/response DTOs (`ListConversationsRequest/Response`, `ConversationCursor`, `GetConversationRequest`, `ConversationDetail`, `DeleteConversationRequest`). Re-export from `lattice-core::lib`.
- [ ] Add `PRAGMA busy_timeout` and `PRAGMA foreign_keys = ON` to `storage/database.rs::open_connection`.
- [ ] Add `create_v5_schema` to `storage/migrations.rs` (conversations/messages tables per design.md), bump `CURRENT_SCHEMA_VERSION` to 5 in `storage/mod.rs`, and add the interrupted-reconciliation `UPDATE` guarded to schema ≥ 5.
- [ ] Add `crates/lattice-core/src/storage/conversations.rs`: `ConversationStore` (`open`/`open_in_memory`), `list`, `get`, `delete`, and the Rust-internal `begin_or_continue`/`start_assistant_message`/`checkpoint_assistant_message`/`finalize_assistant_message` methods used by the chat command handler. Covers conversations spec scenarios for identity, ordering, generation status, pagination, and delete/cascade/conflict.
- [ ] Amend `openspec/changes/local-streaming-chat/{design.md,specs/chat-streaming/spec.md,tasks.md}` in place per this change's design.md "Amendment to 0.8" section: `StartChatStreamRequest`/`ChatRunHandle.conversation_id`, with a note pointing to `conversation-persistence`. No change to `providers::completion`'s `ChatRequest`/`ChatStreamEvent`.
- [ ] Update `apps/desktop/src-tauri/src/lib.rs`: `DesktopState.conversations: Mutex<ConversationStore>` (opened in `.setup()` against the same database path); `start_chat_stream` takes `StartChatStreamRequest`, calls `begin_or_continue` before dispatch, wraps `on_event` with the checkpoint policy, and returns `ChatRunHandle { run_id, conversation_id }`; new `list_conversations`/`get_conversation`/`delete_conversation` commands; `delete_conversation` refuses when `active_chat_run`'s conversation matches (extend `ActiveChatRun` with `conversation_id`). Update `application_command_inventory_matches_tauri_handler` and add `permissions/autogenerated/conversations.toml` + `allow-conversations` in `capabilities/default.json`, updating `tauri_main_window_has_minimal_foundation_capability`'s expected list.
- [ ] Extend `crates/lattice-core/src/wire.rs`'s hand-written `typescript_bindings()` with the new types/commands and the amended `StartChatStreamRequest`/`ChatRunHandle`; regenerate `packages/types/src/generated.ts` via `cargo run -p lattice-core --bin generate_types`; update `packages/types/src/index.ts`'s re-exports; verify `committed_typescript_bindings_match_rust_contract`.
- [ ] Add Rust tests in `storage/conversations.rs` mirroring `storage/settings.rs`/`storage/migrations.rs` conventions: create-on-first-message, list pagination/ordering, reopen after close (temp-file restart persistence), message-history pagination, delete cascades messages, delete-while-active-run conflict, sending-with-stale-local-history conflict, streaming→interrupted reconciliation (drop store mid-stream, reopen, assert `interrupted`), v4→v5 migration against a hand-built old-schema fixture (+ `backup_count` assertion), failed-migration rollback. Add `conversations/mod.rs` tests for title truncation and ID generation.
- [ ] Add `apps/desktop/src/app/core/api/conversations-fallback.ts` (+ `.spec.ts`), extend `app-wire.ts` (`decodeConversationSummary`, `decodeConversationDetail`, `decodeListConversationsResponse`, `normalizeConversationError`) and `app-api.service.ts` (`listConversations`, `getConversation`, `deleteConversation`, updated `startChatStream`). Update `chat-fallback.ts`/`.spec.ts` for the new request/response shape.
- [ ] Extend `core/state/chat.store.ts` with `conversationId`, "new chat" reset, and reopen hydration; add `core/state/conversations.store.ts` for the History list/delete.
- [ ] Add `apps/desktop/src/app/pages/history/` (`history.page.ts/html/css`), a `history` route in `app.routes.ts`, and a nav link in `layout/shell.component.html`; add a "New chat" control to `pages/chat/`.
- [ ] Add/extend Playwright `e2e/app.spec.ts` scenarios: new chat → send → appears in history → reopen → send again → delete; pagination with more than one page of conversations.
- [ ] Write `docs/verification/0.9-conversation-persistence.md` (automated checks run + evidence limits, matching 0.4/0.6/0.7/0.8's format) and update `docs/roadmap.md` (0.9 status line + "Next implementation handoff" pointing at 0.10), `docs/v1/capability-map.md`, `CHANGELOG.md`.
- [ ] Run OpenSpec validation (`pnpm dlx @fission-ai/openspec@1.12.0 validate conversation-persistence --strict`), Rust (`cargo test --workspace`, `cargo clippy --workspace -- -D warnings`), TypeScript (`pnpm test`), web build, E2E, and native (`--no-bundle`) build checks; record results in the verification doc.
