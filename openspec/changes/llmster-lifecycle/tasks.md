# Tasks

- [x] Confirm current `lms daemon`/`server` CLI behavior against a real installed llmster build (version, `--json` shapes, exit codes for down/stop) and update `design.md`'s Documentation sources if behavior differs from the 2026-09-09 doc check. Found: `daemon up --json` emits no JSON and exits 0 on a failed wake (LM Studio.app never launched on this machine); start deadline raised to 90s. Recorded in design.md's "Real CLI behavior" section.
- [x] Split `crates/lattice-core/src/model_runtime.rs` into `model_runtime/{mod.rs,discovery.rs,lifecycle.rs}` with no behavior change to discovery, verified by the existing 0.5 fixture tests still passing unmodified.
- [x] Add `RuntimeOwnership`, `RuntimeOperationOutcome`, `StartModelRuntimeRequest`, `StopModelRuntimeRequest`, `CancelModelRuntimeOperationRequest` to `model_runtime/lifecycle.rs`, plus the `ownership`/`last_operation` fields on `ModelRuntimeStatus` (discovery + serde updates in `discovery.rs`).
- [x] Implement `start_model_runtime`/`stop_model_runtime` ownership-checked argv construction, bounded health-wait/poll loop, and cancellation flag handling in `lifecycle.rs`, reusing `run_probe_command`'s spawn/timeout/kill pattern (generalized into a shared `run_bounded_command` taking a configurable deadline and cancel flag). Covers model-runtime spec scenarios: Runtime Is Started From Stopped, Already Running Runtime Is Not Restarted, Lattice Records Ownership Of A Started Daemon, Pre-Existing Runtime Is Recorded As Attached, Owned Runtime Is Stopped, Attached Runtime Stop Is Refused, Ownership Cannot Be Verified, Health Wait Times Out, User Cancels An In-Flight Operation.
- [x] Confirmed no new `AppError` code is needed: `refused`/`already_running`/`timed_out`/etc. are normal `RuntimeOperationOutcome` values inside a successful `ModelRuntimeStatus` response, not thrown errors, mirroring how `unreachable`/`unsupported` availability is reported today. Existing `runtime_conflict`/`invalid_runtime`/`storage_unavailable` still cover the genuine precondition-failure paths (revision mismatch, no executable configured, storage failure).
- [x] Migrate `crates/lattice-core/src/settings.rs` to schema v3: add `ownership_state`, `owned_daemon_pid`, `owned_since_unix_seconds` columns to the runtime discovery row (reusing the existing `approved_executable_fingerprint` column as the ownership fingerprint, no duplicate column), backed by a pre-migration backup and newer-schema refusal, matching local-storage spec scenarios Ownership Survives Restart When Verified, Unverifiable Ownership Downgrades To Unknown, Settings Storage Migrates To Ownership Schema. `SettingsStore::start_model_runtime`/`stop_model_runtime`/`stop_owned_model_runtime_for_shutdown` added alongside the existing configure/probe methods.
- [x] Add `START_MODEL_RUNTIME_COMMAND`, `STOP_MODEL_RUNTIME_COMMAND`, `CANCEL_MODEL_RUNTIME_OPERATION_COMMAND` Tauri commands in `apps/desktop/src-tauri/src/lib.rs`, the `allow-model-runtime-lifecycle` permission (`permissions/autogenerated/model_runtime_lifecycle.toml` + `capabilities/default.json`), and updated command-inventory/capability-inventory tests. Verified with a full `cargo build -p lattice-desktop` that Tauri's own ACL build step accepts the hand-authored permission file without regenerating it differently. Covers application-api spec scenarios Lifecycle Commands Are Generated, Lifecycle Operation Outcome Is Decoded.
- [x] Add the `tauri::RunEvent::Exit` shutdown hook (`stop_owned_runtime_before_exit` in `lib.rs`, calling `SettingsStore::stop_owned_model_runtime_for_shutdown`) that stops only `owned` resources within the short `SHUTDOWN_STOP_DEADLINE`, covering scenarios Owned Runtime Is Stopped At Shutdown, Attached Runtime Is Left Running At Shutdown. Restructured `run_desktop_shell` to `.build(context)?` + `.run(callback)` instead of the single-shot `.run(context)` to get an exit-event callback.
- [x] Extend generated TypeScript bindings (`packages/types/src/generated.ts`, regenerated via `cargo run -p lattice-core --bin generate_types`, plus re-exporting the new types from `packages/types/src/index.ts` which curates the public surface) and the Angular ModelRuntime API/store/fallback/UI to decode ownership/operation-outcome fields and add Start/Stop/Cancel actions to the Models view, with unhandled-variant decoding falling back to a safe bridge error (`isRuntimeOwnership`/`optionalRuntimeOperationOutcome` in `app-wire.ts`).
- [x] Add disposable-fixture Rust tests for duplicate start (idempotent already-running), timeout/cancellation-in-flight, already-daemonized identity, attached-resource stop refusal, stale-ownership stop refusal, and restart ownership reconciliation (verified-match survives, changed-executable downgrades). Found and fixed two real bugs while writing them (dropped TCP listener before the loopback health check; an unconditional `touch` in a fixture script that defeated its own "already stopped" state) — not flaky-test workarounds, genuine logic bugs in the new tests. Also found and fixed a real, pre-existing flakiness source unrelated to 0.6 logic: `discovery.rs`'s `COMMAND_TIMEOUT` (2s) was occasionally too tight for concurrent subprocess-spawning tests under this sandbox's scheduling load, intermittently failing unrelated 0.5 probe tests too; raised to 5s and confirmed 21 consecutive clean parallel test runs after the fix (was failing roughly 1 in 6-7 runs before). Shutdown-hook stop is exercised at the Tauri layer (see the capability/build task below), not as a `lattice-core` fixture test, since it depends on `tauri::RunEvent`.
- [x] Add focused Angular/E2E tests for Start/Stop UI states: `model-runtime-fallback.spec.ts` covers started/owned, stopped/unknown, and refused outcomes; `e2e/app.spec.ts` adds a full browser-smoke Start→Stop journey through the real Models UI. Fixed an existing `app-wire.spec.ts` fixture that predated the required `ownership` field.
- [x] Add `docs/adr/0010-llmster-lifecycle-ownership.md` recording the ownership/argv/cancellation decisions from `design.md`, and cross-link it from ADR 0005's Consequences.
- [~] Ran the opt-in real llmster smoke as far as this machine allows: `lms daemon up --json` genuinely fails to wake here (see design.md's "Real CLI behavior"), so a full successful owned start/stop and the ten-cycle P1 profile could not be captured. What _was_ verified against the real binary: the failure path returns `Failed`/`TimedOut` without panicking, `lms daemon status --json` reports clean `not-running` afterward, and `ps aux` shows no orphaned process — i.e. the no-orphan guarantee holds even on genuine CLI failure. Recorded as pending, not passed, in `docs/verification/0.6-llmster-lifecycle.md`.
- [x] Run OpenSpec validation, Rust, TypeScript, web build, E2E, and native (`--no-bundle`) build checks; evidence recorded in `docs/verification/0.6-llmster-lifecycle.md`.
