# Tasks

- [ ] Confirm current `lms daemon`/`server` CLI behavior against a real installed llmster build (version, `--json` shapes, exit codes for down/stop) and update `design.md`'s Documentation sources if behavior differs from the 2026-09-09 doc check.
- [ ] Split `crates/lattice-core/src/model_runtime.rs` into `model_runtime/{mod.rs,discovery.rs,lifecycle.rs}` with no behavior change to discovery, verified by the existing 0.5 fixture tests still passing unmodified.
- [ ] Add `RuntimeOwnership`, `RuntimeOperationOutcome`, `StartModelRuntimeRequest`, `StopModelRuntimeRequest`, `CancelModelRuntimeOperationRequest` to `model_runtime/lifecycle.rs`, plus the `ownership`/`last_operation` fields on `ModelRuntimeStatus` (discovery + serde updates in `discovery.rs`).
- [ ] Implement `start_model_runtime`/`stop_model_runtime` ownership-checked argv construction, bounded health-wait/poll loop, and cancellation flag handling in `lifecycle.rs`, reusing `run_probe_command`'s spawn/timeout/kill pattern. Covers model-runtime spec scenarios: Runtime Is Started From Stopped, Already Running Runtime Is Not Restarted, Lattice Records Ownership Of A Started Daemon, Pre-Existing Runtime Is Recorded As Attached, Owned Runtime Is Stopped, Attached Runtime Stop Is Refused, Ownership Cannot Be Verified, Health Wait Times Out, User Cancels An In-Flight Operation.
- [ ] Add `AppError::runtime_not_owned` (or confirm reuse of an existing code) in `crates/lattice-core/src/error.rs` for the `refused` outcome path.
- [ ] Migrate `crates/lattice-core/src/settings.rs` to schema v3: add `ownership_state`, `owned_daemon_pid`, `owned_since_unix_seconds` columns to the runtime discovery row, backed by a pre-migration backup and newer-schema refusal, matching local-storage spec scenarios Ownership Survives Restart When Verified, Unverifiable Ownership Downgrades To Unknown, Settings Storage Migrates To Ownership Schema.
- [ ] Add `START_MODEL_RUNTIME_COMMAND`, `STOP_MODEL_RUNTIME_COMMAND`, `CANCEL_MODEL_RUNTIME_OPERATION_COMMAND` Tauri commands in `apps/desktop/src-tauri/src/lib.rs`, the `allow-model-runtime-lifecycle` permission in `capabilities/default.json`, and update both the command-inventory and capability-inventory tests. Covers application-api spec scenarios Lifecycle Commands Are Generated, Lifecycle Operation Outcome Is Decoded.
- [ ] Add the `tauri::RunEvent` shutdown hook that stops only `owned` resources within a short bounded deadline, covering scenarios Owned Runtime Is Stopped At Shutdown, Attached Runtime Is Left Running At Shutdown.
- [ ] Extend generated TypeScript bindings (`packages/types`) and the Angular ModelRuntime API/store to decode ownership/operation-outcome fields and add Start/Stop/Cancel actions to the Models view, with unhandled-variant decoding falling back to a safe bridge error.
- [ ] Add disposable-fixture Rust tests for duplicate start, occupied endpoint, timeout, crash mid-start, already-daemonized identity mismatch, attached-resource stop refusal, cancellation at each phase, restart ownership reconciliation, and shutdown-hook stop.
- [ ] Add focused Angular/E2E tests for Start/Stop/Cancel UI states (pending, owned, attached-disabled, refused, timed-out).
- [ ] Update `docs/adr/0005-model-runtime-abstraction.md` (or add a new ADR, e.g. `0010-llmster-lifecycle-ownership.md`) recording the ownership/ argv/cancellation decisions from `design.md`.
- [ ] Run the opt-in real llmster smoke (start/stop without loading a model) and record the ten-cycle P1 profile in `docs/verification/0.6-llmster-lifecycle.md`, per Definition of v1's P1 gate.
- [ ] Run OpenSpec validation, Rust, TypeScript, web build, E2E, and native build checks; record evidence and any skipped/blocked checks in the same verification document.
