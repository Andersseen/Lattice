# Tasks

- [ ] Capture one real `lms ls --json` and `lms ps --json` sample against an actual installed model on a machine where the daemon can wake (this development machine cannot currently wake it headlessly; needs either the pending LM Studio.app first-run registration from 0.6 or a different machine). Pin the exact JSON field names `ModelDescriptor`/`LoadedModelObservation` deserialize; update design.md's "Real CLI behavior" if the shape differs from the human-readable example columns in the public docs.
- [ ] Select the exact qualified candidate profiles: one small Qwen model key/quantization and one small Gemma model key/quantization, confirmed present in a real `lms ls --json`/`lms get` catalog read, not guessed from a model family name. Record exact key, quantization, file size and source in the model qualification matrix (`docs/v1/definition-of-v1.md` already requires this format).
- [ ] Add `crates/lattice-core/src/model_runtime/models.rs` with `ModelDescriptor`, `LoadedModelObservation`, `ModelLoadOwnership`, `ModelOperationOutcome`, `ModelSlotStatus`, `list_installed_models`, `list_loaded_models`, `load_model`, `unload_model`, reusing `run_bounded_command` from `discovery.rs`. Re-export from `model_runtime/mod.rs` and `lattice-core::lib`.
- [ ] Implement ownership determination exactly as design.md's "Ownership determination" section specifies: fresh-probe-before-mutate, `Owned`/`Attached`/`Unknown` reconciliation, refuse-while-occupied, confirm-only-via-follow-up-probe. Covers local-models spec scenarios: Model Is Loaded Into An Empty Slot, Loading A Second Model Is Refused While The Slot Is Occupied, Unload Releases Owned Model State, Externally Loaded Model Is Recorded As Attached, Attached Model Cannot Be Unloaded By Lattice, Load Success Is Confirmed Via A Follow-Up Probe, Unconfirmed Load Leaves No Owned Record.
- [ ] Implement the runtime-not-running precondition check (`get_model_runtime_status` read before any `lms` subprocess call) covering scenario Runtime Unavailable Yields An Explicit Unavailable Inventory.
- [ ] Implement bounded deadline/cancellation for load and unload using a new `model_operation_cancelled: Arc<AtomicBool>` independent of 0.6's runtime cancellation flag, covering scenarios Load Times Out Without Assuming Success and User Cancels An In-Flight Load Or Unload. Confirm the provisional 120-second load deadline against the real cold-load timing captured in the P2 task below and adjust design.md/this deadline if wrong.
- [ ] Migrate `crates/lattice-core/src/settings.rs` to schema v4: add a `model_load_state` single-row table (`ownership_state`, `owned_identifier`, `owned_model_key`, `owned_since_unix_seconds`), backed by a pre-migration backup and newer-schema refusal, matching local-storage spec scenarios Load Ownership Survives Restart When Verified, Unverifiable Load Ownership Downgrades To Unknown, Settings Storage Migrates To Model-Load Schema. Add `SettingsStore::get_model_slot_status`/`load_model`/`unload_model` alongside the existing runtime methods.
- [ ] Add `GET_MODEL_SLOT_STATUS_COMMAND`, `LOAD_MODEL_COMMAND`, `UNLOAD_MODEL_COMMAND`, `CANCEL_MODEL_OPERATION_COMMAND` Tauri commands in `apps/desktop/src-tauri/src/lib.rs`, a new `allow-local-models` permission (`permissions/autogenerated/local_models.toml` + `capabilities/default.json`), and update the command-inventory/capability-inventory tests. Covers application-api spec scenarios Model Commands Are Generated, Model Inventory And Outcome Are Decoded.
- [ ] Extend generated TypeScript bindings (`packages/types/src/generated.ts`, regenerated via `cargo run -p lattice-core --bin generate_types`, re-exported from `packages/types/src/index.ts`) and the Angular Models API/store/fallback/UI to decode the new inventory/ownership/operation-outcome fields and add list/select/Load/Unload/Cancel actions, with unhandled-variant decoding falling back to a safe bridge error in `app-wire.ts`.
- [ ] Add disposable-fixture Rust tests for: installed-but-unloaded inventory, duplicate/busy load (slot occupied), insufficient-memory failure (mutating command fails, confirming probe shows nothing loaded), unload-in-use refusal on an attached model, external/attached load detection, runtime-not-running refusal, cancellation at each phase, restart ownership reconciliation (verified-match survives, changed-identity/model-key downgrades), and malformed `ls`/`ps` JSON reporting a safe unknown/failure state rather than panicking.
- [ ] Add focused Angular/E2E tests for the Models view's list/load/unload states, mirroring `model-runtime-fallback.spec.ts`'s coverage pattern and `e2e/app.spec.ts`'s browser-smoke journey style.
- [ ] Add `docs/adr/0011-local-model-load-ownership.md` recording the ownership/confirmation-probe decisions from design.md as a distinct ADR from 0010, cross-linked from it, since this is a new resource type (loaded model) reusing 0010's pattern rather than an extension of the daemon/server ownership 0010 itself covers.
- [ ] Produce the P2 profile (`docs/verification/0.7-installed-model-management.md`): cold load, warm reload, unload timing/memory for each qualified candidate profile, with the 0.6 P1 idle-runtime baseline subtracted. Record exact model ID/quantization, runtime version, and hardware, per the Definition of v1 P2 gate. Record as pending (not passed) if the same headless-daemon-wake blocker from 0.6 prevents a real measurement in a given environment.
- [ ] Run OpenSpec validation, Rust, TypeScript, web build, E2E, and native (`--no-bundle`) build checks; record evidence in `docs/verification/0.7-installed-model-management.md` alongside the P2 profile.
