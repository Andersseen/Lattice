# Tasks

- [ ] Pin the exact `ureq` version and feature set (target: `default-features = false`) that builds against a plain `http://` loopback target with streaming body reads, and confirm the precise API for an idle/per-read timeout versus a total-request timeout. Record the confirmed version, feature flags, and timeout API surface in design.md's "Dependency admission"/"Stream lifecycle" sections if they differ from the provisional entries there.
- [ ] Capture one real SSE sample from `/v1/chat/completions` against an actual installed model on a machine where the daemon can wake (this development machine cannot currently wake it headlessly — the same gap recorded in `docs/verification/0.6-llmster-lifecycle.md` and `docs/verification/0.7-installed-model-management.md`). Confirm the delta JSON shape (`choices[0].delta.content`, `finish_reason`) matches LM Studio's public docs; update `local_openai.rs`'s parser and design.md's "Documentation sources" if it differs.
- [ ] Add `crates/lattice-core/src/providers/mod.rs`, `completion.rs`, `local_openai.rs` with `ChatRole`, `ChatMessage`, `ChatRequest`, `ChatRunHandle`, `ChatFinishReason`, `ChatStreamEvent`, `start_chat_stream`, `cancel_chat_stream`, reusing `model_runtime::models` for the lease check and a new `ActiveChatRun`/cancellation-flag pair independent of 0.6's and 0.7's existing flags. Re-exported from `lattice-core::lib`.
- [ ] Add `AppError::chat_invalid` (`chat.invalid`) and `AppError::chat_conflict` (`chat.conflict`) constructors in `crates/lattice-core/src/error.rs`, following the exact fixed-`&'static str`-message pattern the existing constructors use; reuse `AppError::runtime_conflict` for the unload-while-active-run refusal rather than adding a near-duplicate code. Covers providers/chat-streaming spec scenarios Request Naming Any Other Model Is Refused, A Second Run Is Refused While One Is Active, Oversized Prompt Is Refused Before Any Request Is Sent.
- [ ] Implement the model-lease precondition exactly as design.md's "Model lease and run ownership" specifies: read current `ModelSlotStatus` through the existing settings-store lock, refuse without dispatching when ownership is not `Owned` for the requested `model_key`. Covers chat-streaming/providers scenarios Request Naming The Owned Loaded Model Is Dispatched, Request Naming Any Other Model Is Refused.
- [ ] Implement the single-active-run guard (`DesktopState.active_chat_run`) and the worker-thread panic guard that always clears it on every terminal path. Covers scenarios A Second Run Is Refused While One Is Active, A Crashed Or Errored Run Frees The Active Slot.
- [ ] Implement the SSE read loop in `local_openai.rs`: line-by-line parsing, `data: [DONE]` detection, malformed-line handling, mid-stream cancellation check between reads, and the structural one-`break`-per-terminal-branch shape design.md requires for the "exactly one terminal event" guarantee. Covers scenarios Natural Completion Emits Exactly One Completed Event, Malformed Stream Content Emits Exactly One Failed Event, A Duplicate Terminal-Shaped Payload Is Never Forwarded Twice, Cancel Before Any Delta Is Received, Cancel Mid-Stream, Cancelling An Unknown Or Already-Terminal Run Is A No-Op.
- [ ] Implement and enforce `MAX_OUTPUT_TOKENS`, `MAX_PROMPT_CHARS`, `STREAM_DEADLINE`, and `IDLE_READ_TIMEOUT` constants exactly as design.md's "Stream lifecycle, limits and cancellation" specifies. Covers scenarios Oversized Prompt Is Refused Before Any Request Is Sent, Output Is Bounded By A Maximum Token Count, A Stalled Stream Is Terminated By Deadline.
- [ ] Add the `unload_model` Tauri command precondition in `apps/desktop/src-tauri/src/lib.rs`: refuse with `AppError::runtime_conflict` when `active_chat_run` is `Some`, checked before the settings-store lock. Covers chat-streaming spec scenario Unload Is Refused While A Chat Run Holds The Model Lease.
- [ ] Add `START_CHAT_STREAM_COMMAND`/`CANCEL_CHAT_STREAM_COMMAND` Tauri commands in `apps/desktop/src-tauri/src/lib.rs` (`start_chat_stream` taking a `tauri::ipc::Channel<ChatStreamEvent>`), a new `allow-chat-streaming` permission (`permissions/autogenerated/chat_streaming.toml` + `capabilities/default.json`), the `RunEvent::Exit` best-effort cancel-flag set described in design.md's "Shutdown", and updated command-inventory/capability-inventory tests. Covers application-api spec scenario Chat Commands Are Generated.
- [ ] Extend `crates/lattice-core/src/wire.rs`'s hand-written `typescript_bindings()` (per ADR 0006's Rust-owned-contracts decision — no `ts-rs`) with the new request/handle/event types, regenerate `packages/types/src/generated.ts` via `cargo run -p lattice-core --bin generate_types`, and update the committed-bindings fixture test. Covers application-api spec scenario Chat Commands Are Generated.
- [ ] Add `apps/desktop/src/app/core/api/chat-fallback.ts` (browser-unavailable state, mirroring `model-slot-fallback.ts`), `core/state/chat.store.ts` (in-memory message list, active run ID, streaming/idle/error state, defensive per-event `kind` decoding), and `pages/chat/chat.page.{ts,html,css}` (plain-text message list, send, cancel). Covers application-api spec scenario Stream Events Are Decoded Defensively.
- [ ] Add disposable loopback-fixture Rust tests for: complete stream, fragmented SSE lines split across TCP reads, malformed delta JSON, mid-stream disconnect, duplicate terminal-shaped payload, cancel-before-first-token, cancel-mid-stream, cancel on an unknown/stale run ID, model-mismatch refusal, second-run-while-active refusal, oversized-prompt refusal, idle-timeout termination, and the `unload_model` refusal while a run is active.
- [ ] Add focused Angular/E2E tests for the Chat page's send/streaming/cancel/error states, mirroring `model-runtime-fallback.spec.ts`'s coverage pattern and `e2e/app.spec.ts`'s browser-smoke journey style.
- [ ] Add `docs/adr/0012-local-provider-transport.md` recording the `ureq`/`uuid` dependency-admission decisions and the canonical-port/adapter-isolation boundary from design.md, cross-linked from ADR 0005 (`model-runtime-abstraction`) and ADR 0011.
- [ ] Produce the P2 profile addition (`docs/verification/0.8-local-streaming-chat.md`): active-stream memory/CPU during generation for each 0.7-qualified candidate profile, completing the Definition of v1's P2 gate. Recorded as pending (not passed) if the same headless-daemon-wake blocker from 0.6/0.7 prevents a real measurement in this environment.
- [ ] Run OpenSpec validation, Rust, TypeScript, web build, E2E, and native (`--no-bundle`) build checks; record evidence in `docs/verification/0.8-local-streaming-chat.md` alongside the P2 profile.
