# Tasks

- [x] Pin the exact `ureq` version and feature set. Resolved 2026-09-12: `ureq = { version = "3.4.0", default-features = false }` (Cargo resolves patch `3.4.1`), plus `uuid = { version = "1.26.0", default-features = false, features = ["v4"] }`; both added to `crates/lattice-core/Cargo.toml`, confirmed building with `cargo build -p lattice-core`, and confirmed via `Cargo.lock` to add zero `rustls`/`native-tls` dependencies. `timeout_recv_body` is a total (not idle/per-read) budget, confirmed by reading `ureq` 3.4.1's own source — see design.md's "Stream lifecycle, limits and cancellation" for the resulting reader/orchestrator two-thread cancellation design this required.
- [ ] Capture one real SSE sample from `/v1/chat/completions` against an actual installed model on a machine where the daemon can wake (this development machine cannot currently wake it headlessly — the same gap recorded in `docs/verification/0.6-llmster-lifecycle.md` and `docs/verification/0.7-installed-model-management.md`). Confirm the delta JSON shape (`choices[0].delta.content`, `finish_reason`) matches LM Studio's public docs; update `local_openai.rs`'s parser and design.md's "Documentation sources" if it differs. Also specifically check whether the final chunk is ever dropped — design.md's "Documentation sources" records a reproduced `ureq` 3.4.1 race (multi-write response + zero-delay connection close can lose the last chunk) found via the disposable fixture tests, not yet confirmed either way against a real server.
- [x] Add `crates/lattice-core/src/providers/mod.rs`, `completion.rs`, `local_openai.rs` with `ChatRole`, `ChatMessage`, `ChatRequest`, `ChatRunHandle`, `ChatFinishReason`, `ChatStreamEvent`, `run_chat_stream` (the orchestrator function the Tauri command runs on its own thread). Re-exported from `lattice-core::lib`.
- [x] Add `AppError::chat_invalid` (`chat.invalid`), `AppError::chat_conflict` (`chat.conflict`), and `AppError::chat_failed` (`chat.failed`) constructors in `crates/lattice-core/src/error.rs`, following the exact fixed-`&'static str`-message pattern the existing constructors use. `chat_conflict` is used for the "a response is already streaming" refusal in `start_chat_stream`; the _separate_ `unload_model`-while-streaming refusal reuses the existing `runtime_conflict`, exactly as design.md specifies.
- [x] Implement the model-lease precondition (`authorize_chat_request` in `completion.rs`) exactly as design.md's "Model lease and run ownership" specifies: refuse without dispatching when ownership is not `Owned` for the requested `model_key`, or when the prompt exceeds `MAX_PROMPT_CHARS`. Covers chat-streaming/providers scenarios Request Naming The Owned Loaded Model Is Dispatched, Request Naming Any Other Model Is Refused, Oversized Prompt Is Refused Before Any Request Is Sent. Wired into `start_chat_stream` via `begin_chat_stream` in `apps/desktop/src-tauri/src/lib.rs`, which reads a real `ModelSlotStatus`/`ModelRuntimeStatus` through the existing settings-store lock before dispatch.
- [x] Implement the single-active-run guard (`DesktopState.active_chat_run`, reserved atomically before any precondition check in `start_chat_stream`, rolled back on precondition failure) and the worker-thread panic guard (`ActiveChatRunGuard`, an RAII `Drop` impl constructed on the spawned thread before `run_chat_stream` runs, so the slot is cleared on normal return _and_ while unwinding through an unexpected panic — Rust's default unwind panic strategy runs `Drop` on the way out; confirmed no `panic = "abort"` profile is set anywhere in the workspace). Covers scenarios A Second Run Is Refused While One Is Active, A Crashed Or Errored Run Frees The Active Slot.
- [x] Implement the SSE read loop in `local_openai.rs` (`stream_completion`, run on its own reader thread, pushing `AdapterEvent`s to an `mpsc::Sender`): line-by-line parsing, `data: [DONE]`/`finish_reason` detection, malformed-line handling, and one `AdapterEvent::Failed`/`Completed` push per terminal branch. Covers scenarios Natural Completion Emits Exactly One Completed Event, Malformed Stream Content Emits Exactly One Failed Event, A Duplicate Terminal-Shaped Payload Is Never Forwarded Twice — all covered by disposable loopback-fixture tests (see below).
- [x] Implement the orchestrator poll loop in `completion.rs` (`run_chat_stream`): `mpsc::Receiver::recv_timeout(ORCHESTRATOR_POLL_INTERVAL)`, checking the shared cancellation flag first on every tick, wrapping each forwarded `AdapterEvent` with `run_id`/monotonic `sequence`, and returning immediately on cancellation without waiting for the abandoned reader thread. Covers scenarios Cancel Before Any Delta Is Received, Cancel Mid-Stream, Sequence Numbers Are Monotonically Increasing And Gapless From Zero — all covered by tests, including one proving cancel-mid-stream returns in well under the fixture's 2-second hold, i.e. within `ORCHESTRATOR_POLL_INTERVAL` and not `STREAM_DEADLINE`. Still open: Cancelling An Unknown Or Already-Terminal Run Is A No-Op is a Tauri-command-layer scenario (comparing the requested `run_id` against `DesktopState.active_chat_run`), not something `run_chat_stream` itself decides.
- [x] Implement and enforce `MAX_OUTPUT_TOKENS`, `MAX_PROMPT_CHARS`, `STREAM_DEADLINE` (as `ureq`'s `timeout_recv_body`), and `ORCHESTRATOR_POLL_INTERVAL` constants exactly as design.md's "Stream lifecycle, limits and cancellation" specifies. Covers scenarios Oversized Prompt Is Refused Before Any Request Is Sent, Output Is Bounded By A Maximum Token Count. Still open: a real timed test proving `STREAM_DEADLINE`/`timeout_recv_body` actually terminates a genuinely stalled stream (not just cancellation) — impractical to assert against the real 300-second constant in a fast unit test; needs a parameterized-deadline test or is deferred to the real-runtime smoke task.
- [x] Add the `unload_model` Tauri command precondition in `apps/desktop/src-tauri/src/lib.rs`: refuse with `AppError::runtime_conflict` when `active_chat_run` is `Some`, checked before the settings-store lock. Covers chat-streaming spec scenario Unload Is Refused While A Chat Run Holds The Model Lease. (Precondition implemented and covered by `cargo clippy`/`cargo test -p lattice-desktop` passing; a dedicated fixture test asserting the refusal against a live `active_chat_run` is still open — see the disposable-tests task below.)
- [x] Add `START_CHAT_STREAM_COMMAND`/`CANCEL_CHAT_STREAM_COMMAND` Tauri commands in `apps/desktop/src-tauri/src/lib.rs` (`start_chat_stream` taking a `tauri::ipc::Channel<ChatStreamEvent>`), a new `allow-chat-streaming` permission (`permissions/autogenerated/chat_streaming.toml` + `capabilities/default.json`), the `RunEvent::Exit` best-effort cancel-flag set described in design.md's "Shutdown", and updated `application_command_inventory_matches_tauri_handler`/`tauri_main_window_has_minimal_foundation_capability` tests. Covers application-api spec scenario Chat Commands Are Generated. `cargo build -p lattice-desktop`, `cargo clippy -p lattice-desktop --all-targets`, and `cargo test -p lattice-desktop` (5/5) all pass.
- [x] Extend `crates/lattice-core/src/wire.rs`'s hand-written `typescript_bindings()` (per ADR 0006's Rust-owned-contracts decision — no `ts-rs`) with the new request/handle/event types, regenerate `packages/types/src/generated.ts` via `cargo run -p lattice-core --bin generate_types`, and update `packages/types/src/index.ts`'s re-export list. `committed_typescript_bindings_match_rust_contract` passes. One real finding along the way: `#[serde(rename_all = "camelCase")]` on a tagged enum only renames the tag's variant names, not each struct variant's own fields — `rename_all_fields = "camelCase"` is a separate, additionally-required attribute; caught by a new `chat_stream_event_serializes_with_camel_case_kind_tag` wire-shape test before it ever reached the TS side.
- [x] Add `apps/desktop/src/app/core/api/chat-fallback.ts`, `core/state/chat.store.ts` (in-memory transcript, streaming/error state, model-lease-aware `canSend`), and `pages/chat/chat.page.{ts,html,css}` (plain-text transcript, send, cancel), plus wiring `AppApiService.startChatStream`/`cancelChatStream`, a `chat` route, and a Chat nav link. Deviates from this task's original wording: `chat-fallback.ts` is a real simulated streaming fallback (word-by-word via `setTimeout`, enforcing the same lease/single-run preconditions as the native path) rather than a bare "unavailable" stub — matching the precedent every other capability's own web fallback already set (`model-slot-fallback.ts`, `model-runtime-fallback.ts` fully simulate rather than stub out), which this task's original design.md wording had inconsistently departed from. Covers application-api spec scenario Stream Events Are Decoded Defensively via `decodeChatStreamEvent` returning `null` (not throwing) for a malformed channel payload.
- [x] Add disposable loopback-fixture Rust tests (a small `std::net::TcpListener`-based fixture server, since this is a controlled test double, not the production transport) in `local_openai.rs` for: complete stream, fragmented SSE lines split across TCP reads (which surfaced and documented a real `ureq` 3.4.1 timing edge case — see design.md), `[DONE]`-without-explicit-`finish_reason` completion, malformed delta JSON, mid-stream disconnect, non-2xx status, and a duplicate terminal-shaped payload never being forwarded twice; in `completion.rs` for cancel-before-any-delta (cancel flag pre-set), cancel-mid-stream against a live fixture that holds the connection open, and an unreachable-endpoint failure. 14 tests total, stable across repeated runs, `cargo clippy -p lattice-core --all-targets` clean.
- [ ] Add disposable Rust tests for: cancel on an unknown/stale run ID, second-run-while-active refusal, and the `unload_model` refusal while a run is active — these are `DesktopState`/command-layer scenarios that need a Tauri mock-runtime harness (`tauri::test`) to construct a real `State`/`AppHandle`, a pattern this codebase has not used anywhere yet (0.5–0.7 only unit-test at the `SettingsStore`/core level and cover the Tauri layer via the two inventory/capability JSON-config tests already extended above). The E2E Chat tests below exercise these paths behaviorally through the browser fallback, not the native Tauri command path. A real-timing (parameterized-deadline) stalled-stream test is also still open — see the `STREAM_DEADLINE` task above.
- [x] Add focused Angular/E2E tests for the Chat page's send/streaming/cancel states: `chat-fallback.spec.ts` (fake-timer unit tests: refusal without a loaded model, full started/delta/completed stream, second-run-while-streaming refusal, mid-stream cancel producing exactly one cancelled event, cancel-on-unknown-run-id no-op) and two new `e2e/app.spec.ts` Playwright tests (load a model, navigate to Chat, send a message, watch it stream and complete; send a message and cancel it mid-stream), run against a real headless Chromium browser via `pnpm --filter @lattice/desktop e2e` — 8/8 passing. `app-wire.spec.ts` also extended with `decodeChatRunHandle`/`decodeChatStreamEvent`/`normalizeChatError` coverage, including the "malformed event decodes to null, not a throw" case.
- [ ] Add `docs/adr/0012-local-provider-transport.md` recording the `ureq`/`uuid` dependency-admission decisions and the canonical-port/adapter-isolation boundary from design.md, cross-linked from ADR 0005 (`model-runtime-abstraction`) and ADR 0011.
- [ ] Produce the P2 profile addition (`docs/verification/0.8-local-streaming-chat.md`): active-stream memory/CPU during generation for each 0.7-qualified candidate profile, completing the Definition of v1's P2 gate. Recorded as pending (not passed): the same headless-daemon-wake blocker from 0.6/0.7 prevents a real measurement in this environment.
- [x] Run OpenSpec validation, Rust, TypeScript, web build, E2E, and native (`--no-bundle`) build checks; record evidence in `docs/verification/0.8-local-streaming-chat.md` alongside the P2 profile. All automated checks pass; see that document for the full list and for what remains genuinely pending (real llmster SSE sample, P2 profile, Tauri-command-layer disposable tests, native GUI smoke).
