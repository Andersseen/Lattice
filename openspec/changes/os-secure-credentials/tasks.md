# Tasks

- [ ] Pin `security-framework = { version = "3.7.0", default-features = false }` in `crates/lattice-core/Cargo.toml`; confirm `cargo build -p lattice-core` compiles and `Cargo.lock` adds only `security-framework`/`security-framework-sys`.
- [ ] Add `AppError::credential_cancelled` (`credential.cancelled`), `AppError::credential_not_found` (`credential.not_found`), `AppError::credential_unavailable` (`credential.unavailable`), and `AppError::credential_unsupported` (`credential.unsupported`) constructors in `crates/lattice-core/src/error.rs`.
- [ ] Add `crates/lattice-core/src/credentials/mod.rs`: `CredentialAvailability`, `CredentialRef`, `SecretStoreStatus`, `PromptOutcome`, the `SecretStore`/`SecretPrompt` traits, ID generation, label/provider-key validation, IPC request DTOs, and `FakeSecretStore`/`FakeSecretPrompt` (`#[cfg(test)]`).
- [ ] Add `crates/lattice-core/src/credentials/macos.rs` (`#[cfg(target_os = "macos")]`): `MacosKeychainSecretStore` (via `security_framework::passwords`, fixed service constant, lock-status via `SecKeychain::default().status()`) and `OsascriptSecretPrompt` (fixed non-interpolated prompt text, dedicated reader thread, ~125s hard timeout, `-128`/"User canceled" → `Cancelled`).
- [ ] Add `crates/lattice-core/src/credentials/unsupported.rs` (`#[cfg(not(target_os = "macos"))]`): `UnsupportedSecretStore`/`UnsupportedSecretPrompt`, both fail-closed.
- [ ] Add `create_v6_schema` to `storage/migrations.rs` (`credentials` table per design.md), bump `CURRENT_SCHEMA_VERSION` to 6 in `storage/mod.rs`.
- [ ] Add `crates/lattice-core/src/storage/credentials.rs`: `CredentialStore` (`open`/`open_in_memory`, generic/boxed over `SecretStore`), `list`, `create`, `replace`, `delete`, `resolve_secret` (Rust-internal, not IPC). Covers the spec's rollback/availability/cancellation scenarios.
- [ ] Add a `#[cfg(target_os = "macos")]` real-keychain test module in `storage/credentials.rs` or `credentials/macos.rs` round-tripping `MacosKeychainSecretStore` against disposable UUID-suffixed accounts with guaranteed cleanup.
- [ ] Update `apps/desktop/src-tauri/src/lib.rs`: `DesktopState.credentials: Mutex<CredentialStore>` (opened in `.setup()` against the same database path, real `MacosKeychainSecretStore`/`OsascriptSecretPrompt` on macOS, `Unsupported*` elsewhere); `list_credentials`/`create_credential`/`replace_credential`/`delete_credential` commands. Update `application_command_inventory_matches_tauri_handler`, add `permissions/autogenerated/credentials.toml` + `allow-credentials` in `capabilities/default.json`, update `tauri_main_window_has_minimal_foundation_capability`'s expected list.
- [ ] Extend `crates/lattice-core/src/wire.rs` with the new types/commands; regenerate `packages/types/src/generated.ts`; update `packages/types/src/index.ts`; verify `committed_typescript_bindings_match_rust_contract`, including a dedicated assertion that `CredentialRef`'s generated TS shape has no secret-shaped field.
- [ ] Add Rust tests in `storage/credentials.rs` per design.md's "Security invariants": raw-SQLite-bytes-never-contain-the-secret, cancellation-leaves-no-trace, keychain-write-failure-rolls-back-the-row, all four availability states, delete removes both sides, v5→v6 migration fixture.
- [ ] Add `apps/desktop/src/app/core/api/credentials-fallback.ts` (+ `.spec.ts`) simulating create/list/replace/delete with a fake prompt; extend `app-wire.ts`/`app-api.service.ts`.
- [ ] Extend the Settings page (`pages/settings/`) with a "Credentials" section (list/status badges/add/replace/delete); add a small `core/state/credentials.store.ts`.
- [ ] Add a Playwright e2e scenario: add a credential (fake prompt in web fallback), see it listed as available, replace it, delete it.
- [ ] Add `docs/adr/0013-native-macos-credential-storage.md` recording the `security-framework`/`osascript` dependency-admission decisions from design.md.
- [ ] Write `docs/verification/0.10-os-secure-credentials.md` (automated checks run + evidence limits, matching 0.9's format) and update `docs/roadmap.md`, `docs/v1/capability-map.md`, `CHANGELOG.md`.
- [ ] Run OpenSpec validation (`pnpm dlx @fission-ai/openspec@1.12.0 validate os-secure-credentials --strict`), Rust (`cargo test --workspace`, `cargo clippy --workspace -- -D warnings`), TypeScript (`pnpm test`), web build, E2E, and native (`--no-bundle`) build checks; record results in the verification doc.
