# Tasks

- [x] 1. Enable `ureq`'s `rustls` feature in `crates/lattice-core/Cargo.toml` (keep `default-features = false`); confirm `Cargo.lock` adds exactly the crates listed in design.md "Dependency admission", verify each new crate's license from its manifest, and build `lattice-core`. Verification: `cargo build -p lattice-core`, lockfile diff recorded in the verification doc. Done 2026-09-16: lockfile adds exactly the 8 listed crates plus `windows-sys` 0.52.0, which `ring` only uses on Windows ARM64; licenses recorded in ADR 0014.
- [x] 2. Add `provider.*` `AppError` constructors (`crates/lattice-core/src/error.rs`). Covers the providers "Normalize Remote Provider Failures" requirement's codes. Done.
- [x] 3. Add `crates/lattice-core/src/providers/profiles.rs`: DTOs, command constants, `REMOTE_PROVIDER_KEY`, `validate_endpoint`/label/model validation, `BearerToken`, `prepare_remote_target`. Unit tests: endpoint accept/normalize/refuse table; model mismatch, missing consent, stale consent endpoint, credential resolver errors propagated, unusable token bytes — each refused before any target is built. Covers "Accept Only Explicit HTTPS Remote Endpoints", "Contact A Remote Provider Only For An Explicit, Consented Chat Request". Done: 11 tests.
- [x] 4. Add schema version 7 (`storage/migrations.rs` `create_v7_schema`, `CURRENT_SCHEMA_VERSION = 7`, sanity read) and `storage/provider_profiles.rs` `ProviderProfileStore` (`open`, `list`, `get`, `create`, `update`, `delete`, `bind_credential`, `grant_consent`, `revoke_consent`). Tests: CRUD, revision conflicts, profile cap, unknown credential binding refused, credential deletion unbinds (FK), endpoint change clears binding + consent, label/model change keeps them, consent endpoint mismatch refused, raw SQLite bytes contain no secret, v6→v7 migration fixture with backup, no-network listener test. Covers "Store Remote Provider Profiles As Non-Secret References", "Bind Credentials And Consent To One Destination", "Profile Management And Selection Send No Network Traffic". Done: 17 tests, including credential deletion through the real `CredentialStore` connection unbinding profiles via the foreign key.
- [x] 5. Rename `providers/local_openai.rs` → `providers/openai_compatible.rs`; introduce `OpenAiCompatibleTarget` (local/remote), destination URL/auth/limit-field selection, `max_redirects(0)`, `proxy(None)`, `http_status_as_error(false)`, bounded line reader, `data:` tolerance, in-stream `error`, `delta.refusal`, empty-assistant omission, timeout mapping and remote status normalization; pass the cancellation flag to the reader (skip dispatch / stop at next line). Keep every existing local fixture test green. New fixture tests per design.md "Verification". Covers "Reach Only The Configured Remote Destination", "Normalize Remote Provider Failures", "Translate The Pinned OpenAI-Compatible Subset", "Keep Remote Credentials Out Of Every Non-Request Surface", chat-streaming "Cancel Remote Runs Promptly …". Done: 23 adapter tests — 21 by default (7 original local tests kept) plus 2 opt-in network tests. Found and fixed: `ureq` reports a body deadline as `io::Error` wrapping `ureq::Error::Timeout`, not `ErrorKind::TimedOut`.
- [x] 6. Amend `providers/completion.rs`: `ChatTarget`, `StartChatStreamRequest.target`, `run_chat_stream(target, …)`; wire-shape test for `ChatTarget` serialization; shared-history test driving the same `ChatRequest` through local and remote fixture destinations. Covers chat-streaming "Resolve The Chat Target Per Request". Done. Also fixed a cancellation race: a reader exiting on cancel could make the orchestrator report `failed` instead of `cancelled`.
- [x] 7. `storage/conversations.rs`: `start_assistant_message(conversation_id, provider_key, model_key)`; test local → remote → local provenance survives reopen. Covers conversations "Record Provider And Model Provenance Per Assistant Message". Done.
- [x] 8. Tauri shell (`apps/desktop/src-tauri/src/lib.rs`): `ProviderProfileStore` state, seven commands, target-branching preconditions with one-store-at-a-time locking, `holds_local_model_lease` for `unload_model`; `permissions/autogenerated/provider_profiles.toml`, `allow-provider-profiles` capability; update inventory/capability tests. Covers application-api "Extend Checked IPC Contracts For Remote Provider Profiles", chat-streaming "Hold The Local Model Lease Only For Local Runs". Done. Still open: no Tauri command-level harness exists (same gap as 0.8), so `resolve_chat_target` and the `unload_model` lease check are covered by core tests, inventory/capability tests and the release build, not a command test.
- [x] 9. Contracts: extend `wire.rs`, regenerate `packages/types/src/generated.ts` (`bun run contracts:generate`), export new types from `packages/types/src/index.ts`; `contracts:check` passes. Done.
- [x] 10. Angular API: `app-wire.ts` decoders/normalizer (+ spec), `app-api.service.ts` methods and `startChatStream` target, `provider-profiles-fallback.ts` (+ spec), remote branch in `chat-fallback.ts` (+ spec), provenance-aware `conversations-fallback.ts`. Done.
- [x] 11. Angular state/UI: `core/state/provider-profiles.store.ts`; `ChatStore` target selection, consent-gated send, provenance on transcript entries; Settings remote-provider section; Chat selector, disclosure dialog and provenance labels (Volt UI only, keyboard-accessible native selects and dialog). Done. The Chat header select uses `aria-label="Provider"` because a plain `<label for>` did not name `select[voltNativeSelect]`.
- [x] 12. Playwright: Settings profile add/edit(endpoint change unbinds + clears approval)/rebind/remove journey; Chat local → remote (disclosure dialog, cancel sends nothing, allow sends) → local, then reopen shows all messages with provenance. Done: 11/11 E2E passing on three consecutive runs.
- [x] 13. Opt-in real smokes in `openai_compatible.rs` (`#[ignore]`, env-gated): unauthenticated HTTPS → `provider.auth_failed`; authenticated streaming smoke. Run the unauthenticated one here; record the authenticated one as pending without a maintainer credential. Done: the unauthenticated smoke passed against `https://api.openai.com/v1` (`provider.auth_failed`). Still open: the authenticated smoke, blocked on no maintainer credential in this environment.
- [x] 14. Docs: `docs/adr/0014-remote-provider-egress.md`; `docs/remote-providers.md` (egress policy, pinned subset, conformance matrix, setup); `docs/verification/0.11-remote-openai-compatible-chat.md`; update `docs/roadmap.md` (status + next handoff 0.12), `docs/v1/capability-map.md`, `docs/architecture.md`, `docs/v1/architecture-sequence.md` status line, `docs/development/spec-driven-development.md` handoff, `CHANGELOG.md`, `README.md`, `SECURITY.md` where they describe current capabilities. Done.
- [x] 15. Verify: `bunx @fission-ai/openspec@1.12.0 validate remote-openai-compatible-chat --strict`, `bun run openspec:validate`, `bun run check`, `bun run build:web`, `bun run e2e`, `bun --filter @lattice/desktop tauri:build`; record results, skipped/pending evidence and the candidate state in the verification doc. Done: see `docs/verification/0.11-remote-openai-compatible-chat.md`. Final archive remains blocked on the pending evidence listed there.
